# Subagent

The main reason for `subagent` is that it has its own context window (saving token in the context window of main agent).


@agent

there are built-in subagent

create agent with

```bash
/agents
```