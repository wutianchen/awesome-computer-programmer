# Go


## Language Features

* [named return value and naked return](./features/naked_return.md): name return values in the signature to document what each returned value represents, without the caller needing to read the function body.